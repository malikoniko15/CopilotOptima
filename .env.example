# Без этих значений интерфейс работает в явно обозначенном деморежиме.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# В разработке /api перенаправляется Vite на localhost:8000.
# В production задайте HTTPS-адрес вашего backend (без /api).
VITE_API_URL=
